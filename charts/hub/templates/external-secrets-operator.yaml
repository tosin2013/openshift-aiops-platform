{{- if eq .Values.secrets.backend "external-secrets" }}
---
# NOTE: External Secrets Operator is installed cluster-wide via OperatorHub
# This template only creates namespace-scoped resources

# ServiceAccount for External Secrets Operator
# NOTE: ESO Operator is installed cluster-wide via OperatorHub
# No operator deployment needed in this template

{{- end }}
